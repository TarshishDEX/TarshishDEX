/**
 * Re-export of the cn utility for consistent import paths.
 * Components can import from @/lib/utils/classnames instead of @/lib/utils
 * when they only need the className merge utility.
 */
export { cn } from "@/lib/utils";
